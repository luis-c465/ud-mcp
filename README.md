# Universal Database MCP Server

Minimal project scaffold for the Universal Database MCP Server.

## Status

Project bootstrap in progress.

## Dependency extras

- `dev` — local testing and linting tools (`pytest`, `pytest-cov`, `ruff`, `mypy`)
- `databricks-sqlalchemy` — optional SQLAlchemy dialect support for Databricks
